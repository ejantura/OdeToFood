@model IEnumerable<OdeToFood.Models.ResteurantReview>

@{
    ViewBag.Title = "Index";
}

<h2>The Latest Reviews</h2>

Whe are showing the lastest @Model.Count() reviews.

@foreach (var item in Model)
{
    @Html.Partial("_Review", item)
}
<p>
    @Html.ActionLink("Create New", "Create")
</p>
