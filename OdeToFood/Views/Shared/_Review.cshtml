@model OdeToFood.Models.ResteurantReview

<div>
    <h4>@Model.Name</h4>
    <span>@Model.Rating</span>
    <p>@Model.City,@Model.Country</p>
    <span>
        @Html.ActionLink("Edit", "Edit", new { id = Model.Id })
    </span>
</div>